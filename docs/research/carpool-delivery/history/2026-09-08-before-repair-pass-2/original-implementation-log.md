> 2026-09-08 继续实施进度：本文件下方为较早阶段记录，不能代表当前最终状态。现已接通三阶段服务、SQLite/PostgreSQL 持久请求与组、Native 园区 MLS、请求中心/聊天、地图选点与脱敏预览、生命周期清理。当前仍在进行分页接线、完整 UI 联调、无障碍及独立自查；最终矩阵和报告将在验收后整体重写。新增服务端专项 75 项通过；完整 React UI→真实 SQLite→Native MLS 链路已实际执行通过，使用明确的本地地图 Provider 测试夹具，不代表真实高德或 OS 真机验收。

# 拼车实施记录

## 基线

2026-09-08，internal / 93dcf8ea2dd0e9582589351a6e126ecd24b33b86。原始 git 差异已保存在 /tmp/otto-carpool-existing.patch，状态在 /tmp/otto-carpool-existing-status.txt；不提交、不推送、不部署。原有 9 个修改文件、docs/research 与 native 未跟踪文件均保留。

- doctor：失败，60.68 MB > 50 MB；未调整阈值或清理用户文件。
- git diff --check：基线通过。
- 首批新增回归：20 项中 9 失败、11 通过，证据 /tmp/otto-carpool-red.log。
- 修复有向区间重合、日期一致性、候选私密标签泄露与规划后身份/状态重新校验后，20 项通过。
- 独立只读审查确认现有 MLS 严格二人同企业，必须独立建立园区群协议，不能放松原隔离规则。当前尚未完成阶段一至三。

## 本批实际修改

- 重合计算改为方向筛选、投影区间裁切与合并；补局部重合、交叉/反向/平行、不同密度与共同里程上界。
- 输入拒绝无效日期、无时区时间和北京时间日期不一致。规划后重新验证身份/园区/当前意向，数据库 CAS 防止旧规划覆盖。
- SQLite 添加 version 和加密 publications 表；PostgreSQL 复用 business records 实现 publication，抽取专用 store；发布意向与回执同事务，故障注入验证回滚。
- 候选逐页召回，坏密文和坏几何隔离；当前仍无全局计算预算和持久重试。
- 新增主动确认 API 并贯通 HTTP、EnterpriseClient、Main IPC、preload、面板；刷新不续命。
- 对话修复下午时间、浮动字段覆盖、歧义选点、取消、重复确认、失败恢复及版本绑定；草稿纳入原加密 vault。
- 面板增加轻量筛选、新结果计数提示、未保存退出、真实暂停/过期状态；消费发布/停止/确认回执时保持字段与版本一致。
- 新增真实双数据库契约、事务失败注入、HTTP 和组件/对话→真实服务→数据库契约；不使用模拟地图结果充当真实地图验收。
- 对独立 reviewer 发现的斜线密度、身份不一致、回执原子性、草稿/表单版本和坏候选问题逐个补测修复。
- 追加短绕行抽样回归后移除128点等间隔抽样；形状简化保留方向反转锚点，修复 reviewer 再次发现的直线折返丢失。Provider超限/无效几何明确拒绝。

## 最终状态

服务端选定107项、桌面选定102项、原生MLS18项通过；类型与构建、相关文件lint、diff、code-map通过。doctor源码预算与既有policyIntelligencePresentation.test.ts跨包源路径边界仍失败，原始日志随报告保存。精确命令及实测边界见 final-self-review.md。

本次不包含完整陌生消息、邀请、两人组、多人成组或园区MLS协议实现。阶段一部分完成、阶段二三未实现；整体验收不通过，不应关闭完整实施任务。未提交、推送或部署；未改动原私聊同企业授权约束。
