# 阶段 0 执行记录 — 2026-09-08

结论：源码与评分规则已冻结，本机隔离源码复现通过；真实模型版本、价格、费用上限及专用凭据未确认，完整真实模型基线尚未建立。没有推送、部署、发布或生产数据操作。

## 固定快照

- 目录：`D:\otto\agent-baselines\stage0-20260908-003`
- 冻结时间：2026-09-08 08:22:36（Asia/Shanghai）。
- 来源分支：`internal`；HEAD：`93dcf8ea2dd0e9582589351a6e126ecd24b33b86`。
- 实际源码：2,356 个文件、40,587,063 字节（约 40.6 MB）；75 个修改文件、73 个未跟踪文件已包含。HEAD 不能代表这些未提交内容。
- 源码清单 SHA-256：`c333a6ce833c937900d911051b6bafc2475152be030752a53fd50c1549afa5a5`。
- 评分/测试夹具指纹：`3cecfc1f95feaa92615d72940744a2ee66695c05527ec55548560a308f557f14`。
- Node：v24.17.0；Windows 内核：10.0.26200；另记录了 Node 可执行文件哈希、架构、时区、锁文件及 1,276 个本机依赖 package.json 的版本/哈希。
- 319 个锁文件路径在本机未安装，包含平台可选项或当前布局下不存在的依赖位置；没有将其伪装为完整依赖重建成功。本次运行复用现有第三方依赖，不做安装或完整字节归档。

完整清单：[manifest.json](/D:/otto/agent-baselines/stage0-20260908-003/manifest.json)。冻结源码与恢复出的测试源码在两次运行后均通过完整性校验。

该快照是上述时间点的版本，不会跟随其他任务后续编辑自动变化。本次新增执行记录也不在已经冻结的源码清单中。

## 验证记录

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| 仓库 doctor | 通过 | 当前环境检查，不是完整产品验收 |
| 基线工具专项 Vitest | 23/23 通过 | 覆盖脏工作树、未跟踪/删除文件、敏感文件、目录联接、同长度篡改、不可比模型/价格/规则/环境、缺失报告等 |
| Evals 类型检查 | 通过 | `npm run typecheck --workspace=packages/evals` |
| 本次改动 ESLint 与差异检查 | 通过 | 本阶段涉及的脚本、测试，以及 `git diff --check` |
| 冻结源码隔离运行 1 | 84/84 通过，0 跳过 | 新进程、新恢复目录；20.510 秒 |
| 冻结源码隔离运行 2 | 84/84 通过，0 跳过 | 再次新进程、新恢复目录；23.790 秒 |
| 真实模型评测 | 未运行 | 付费请求 0；真实模型质量分数为空 |

两次运行均含 9 项脚本化本机回环提供方任务，通过真实 CoreSessionRuntime 加载、文件读写、独立测试命令和完成流程。其余是确定性需求、任务图、压缩、验收、修复及评分测试。这些结果不等同于“84 个真实用户任务成功”，不代表完整 Electron GUI、企业线上工单或 WPS 操作实测。

原始回执：

- [运行 1](/D:/otto/agent-baselines/stage0-20260908-003/runs/offline-WjN6Rl/results/result.json)
- [运行 2](/D:/otto/agent-baselines/stage0-20260908-003/runs/offline-2ZEndi/results/result.json)

## 保留的失败与处理

1. `stage0-20260908-001`：根目录依赖恢复后，3 个测试文件因子包依赖未接入而不能加载；已修正**评测恢复脚本**，旧失败记录保留。
2. `stage0-20260908-002`：80 通过、4 失败，发生在功能组删除、客服回复、验收修订、目标变更四个**合成数据测试**。独立输出 oracle 都正确，但脚本化提供方遗漏负向测试的 `expectedOutcome`，导致需求覆盖未完成。只补齐夹具中的该标记，不修改产品验收规则。
3. 加入标记后生成 `003` 新快照并完整重跑两次。没有重写旧结果，也没有把先前失败计作通过。测试夹具指纹随改动变化；如果以后已有真实成绩，不能将这类评分夹具变化混入运行时效果提升。

本次业务 Agent 关键文件 taskRequirements、taskContract、agentTurnTracker、runtime、adaptiveExecution、taskGraph、server、deliveryRepair、turnControlPolicy 在准备前后哈希相同。招聘模块有其他并行工作产生的变更，快照按冻结时内容保存，本次未修改那些文件。

## 阶段 0 剩余项

已登记 8 类独立验收合同，6 项开发、2 项保留；真实 GUI/修复/纠偏等新端到端适配器仍按后续计划实施，登记不算运行通过。

真实对照前仍需确认：模型固定版本/修订、专用端点、输入输出计费口径、总费用上限和专用凭据。当前 `manifest.protocol.live.ready=false`，不能声称“模型和预算已固定”。不要将密钥发到聊天或写进源码。

获得这些信息后，以新比较协议同时约束旧快照和新版；配置提供方硬账单上限，核查真实 harness 的 token/费用上限再运行。可以先进入阶段 1 的本地安全修复，但必须保留本快照以便回跑。

复现方式与范围说明：[阶段 0 基线说明](/D:/otto/otto-push-20260901/docs/agent-stage0-baseline-20260908.md)。
