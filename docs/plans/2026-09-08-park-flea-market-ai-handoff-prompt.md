# 园区跳蚤市场开发交接提示词

以下整段可复制给另一位 AI。**本文件的生成不代表已执行开发；只有接收此提示词并获得开发任务的 AI 才执行。**

---

你是这个项目的全栈开发负责人。请在现有代码中完整实现“园区跳蚤市场”，不要停在分析、方案、静态页面或接口占位。按照下面的 PRD 和执行计划逐任务开发、验证、修复，并产出最终自查报告。

## 必须先读取

1. 仓库：`/Users/yang/Desktop/otto-new`。
2. 仓库规则：`/Users/yang/Desktop/otto-new/AGENTS.md`，以及实际修改目录适用的 AGENTS.md。
3. 产品基线：`/Users/yang/Desktop/otto-new/docs/plans/2026-09-08-park-flea-market-prd.md`（v1.1，全文，包括字段、权限、时限、非功能要求和 A01–A40）。
4. 执行计划：`/Users/yang/Desktop/otto-new/docs/plans/2026-09-08-park-flea-market-implementation-plan.md`，从 Task 0 开始。
5. `/Users/yang/Desktop/otto-new/docs/code-map.md`、`/Users/yang/Desktop/otto-new/docs/product-modules.md`、`/Users/yang/Desktop/otto-new/docs/runtime-kernel-boundary.md`。
6. 技能：`/Users/yang/.agents/skills/superpowers/executing-plans/SKILL.md` 和 `/Users/yang/.agents/skills/superpowers/verification-before-completion/SKILL.md`。若环境不存在这些技能，说明缺失并遵守本提示词和计划的同等流程；不要宣称读过不存在文件。

以 PRD 为产品事实，执行计划为工程路径。计划中的新文件、接口和测试是拟定设计，不是已经实现的能力。发现代码已被其他人更新时先核对再复用，不能用计划中的旧状态覆盖新实现。PRD 与现有安全/数据策略存在实质冲突时写明具体冲突、采取不损坏既有功能的方案，并继续不受影响的任务，不擅自删减首发需求。

## 你要交付的完整范围

- 同园区跨企业的闲置发现、分类搜索排序、详情、收藏和内部分享。
- 图片选择/拖拽/粘贴、HEIC/HEIF 真实转码、单图错误恢复、受控图片访问、发布编辑和账号隔离本机草稿。
- 出售/免费送、成色及功能状态、故障说明、预留和本人私有备注、预计交接时间修改、下架、售出、撤销、重新上架和有效期。
- 首条问题与商品快照、陌生联系请求、接受/回复/忽略/撤回/到期、已有会话复用、屏蔽、按商品历史授权、会话关联商品列表。
- 所有咨询和通知真实进入“我的消息”，服务器持久化、离线/重启可恢复、已读跨设备同步，桌面通知遵循偏好。
- 本人发布记录在模块移除、市场暂停、退出园区后仍有可达管理入口，动作及历史图片访问按最新资格限制。
- 举报、限定证据、管理员工作台、移除、发布限制、恢复、申诉、规则/责任人配置、审计和结果通知。
- SQLite 与真实 PostgreSQL 一致的业务行为、幂等/额度/并发/任务恢复/数据保留/访问撤销，macOS 和 Windows 验收。

不要新增支付、订单、物流、库存、评价、跨园区、求购、手机扫码、AI自动发布或企业资产处置。这些不是首发交付条件，也不能用做了额外功能抵扣首发缺项。

## 开发原则

1. 先记录工作区和环境基线。仓库有其他开发者的拼车及其他未提交工作，不 reset/clean/stash/drop，不擅自 pull，不覆盖共享文件。只修改本任务相关内容，必要时按执行计划安全隔离工作区。
2. 不把市场逻辑塞入 Agent 内核，不套报修工单，不在 App.tsx、main/index.ts 或 db.ts 继续堆整套业务；这些文件仅做适当装配。
3. 当前企业私信存在同企业限制，不能删除组织隔离来支持跨企业咨询。新增显式园区授权并保留企业私聊/MLS 等安全模式，验证无权限泄露和回归。
4. 图片要真实可上传、转码、读取和撤权。过期时间短的裸公开地址不等于立即撤权，mock 图片也不等于 HEIC 已支持。
5. 每个业务规则先写失败测试，确认是预期规则缺失，再最小实现并跑绿。按任务记录精确文件、命令和结果，局部提交只含自己的改动。不 push、合并、部署生产或发送真实用户消息。
6. 测试 fixture 可以模拟外部失败，但服务/数据库/权限的端到端验收必须走真实链路。不得使用内存 Map、演示成功响应或仅前端状态冒充持久化交付。
7. 核心 v1.1 决策不得遗漏：预留跨期显式续期；误恢复保留原排名和期限；请求终止不自动复活；历史授权按商品；同物编辑；草稿租约与清理；独立本人管理入口；首次咨询及已有会话新商品发送均幂等。
8. 功能能力与园区开关必须准确，依赖未就绪保持生产关闭。缺真实平台/数据库/存储/运营责任人时明确列为未验证或上线阻塞，继续可完成工作，不能要求用户一次次批准普通可逆实现选择。

## 必须完成的自检

逐项核验 PRD F01–F07、A01–A40 和字段/非功能要求，运行执行计划中的测试、两包类型检查、必要构建、边界检查和代码地图检查。对失败定位根因、修复并复测。必须实际用同园区不同企业的两个测试账号走通发布→咨询→回复→预留→售出，再使用第三方、跨园区及停用账号验证隔离。补测断网重试、两设备冲突、服务重启、请求到期、治理、内容清理与退出园区。

按执行计划生成：

- `/Users/yang/Desktop/otto-new/docs/research/2026-09-08-park-flea-market-self-check.md`
- `/Users/yang/Desktop/otto-new/docs/research/2026-09-08-park-flea-market-bugs.md`
- `/Users/yang/Desktop/otto-new/docs/research/2026-09-08-park-flea-market-execution-log.md`
- `/Users/yang/Desktop/otto-new/docs/plans/2026-09-08-park-flea-market-technical-decisions.md`

自查每行必须给出实现位置、测试/操作证据、实际结果和状态，不能只打勾。这些文件目前是待生成交付物，不应假定已有报告。最终回答说明实现内容、验证结果、未通过/未验证项、外部上线条件和报告路径。没有实际运行证据的项不能写通过；不要因为工期、上下文或 token 不足删需求并宣布完成，先留下精确进度让下一轮接续。

现在先读取上述文件，完成 Task 0 的基线与技术决策，然后按执行计划执行开发。除实际无法继续的外部条件或实质需求冲突，不要只返回计划后停止。
