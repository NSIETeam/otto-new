# 后续现场验收清单（本轮用户明确不要求执行）

以下保留为将来产品/上线验收的边界，不作为此次代码修改的待办阻塞，也不预填“通过”。仅用专用测试账号/公共点位，不给真实员工发测试消息。

- [ ] 配置授权的 `OTTO_AMAP_WEB_SERVICE_KEY`，确认供应商缓存/衍生计算使用条件；运行 `node packages/desktop/scripts/carpool-acceptance/live-map.mjs`。公共杭州“市民中心→江陵路地铁站”，搜索/逆编码/静态图/GPS转换/驾车/真实腿段绕行；缺key退出2，失败退出1。不要将住宅细址或key写日志。
- [ ] 在专用已配置地图的企业部署启用单园区试点，三家企业A/B/C的专用批准设备登录完整App；从模块超市/右栏发布当日公共点位意向；B收请求并接受聊天；双方文本收发，核对独立于组队。
- [ ] A发明确方式/司机/容量邀请，B接受形成两人；C申请入组，经授权协调人接受；验证C看不到加入前消息、离开者看不到后续内容；明确转交接受、退出/保留组停止选择。
- [ ] 移除模块时提示且“我的消息”仍可管理当前状态，再添加不丢布局；panel/page、折叠、关闭重开、应用重启恢复。
- [ ] 断网、丢响应、双击、切账号、其他设备修改意向时不重发/复活旧状态；企业私聊筛选/未读/附件/联邦联系人保持正常。
- [ ] 通知关闭/开启、点击会话、App后台/完全退出；记录实际行为，不假定后台推送服务存在。
- [ ] macOS与Windows分别记录OS/架构/build：定位允许/拒绝/系统变更、仅主动点击才请求权限；不放宽外部窗口、摄像头/视频。键盘/焦点、缩放与读屏逐操作验证。
- [ ] 双数据库测试副本升级/备份恢复、跨节点故障与负载、保留时间/阈值/人数、举报责任人、身份退出处置签认。

自动化入口说明：`normal-app-smoke.mjs` 用一次性本地签名许可证和SQLCipher临时数据库启动实际main/preload/App，当前只证明标准启动与单账号登录，不自动完成上面的三人旅程。需可信、匹配Electron ABI的 `OTTO_SQLCIPHER_NATIVE_BINDING`；macOS本次运行使用已存在本地资产，脚本中不硬编码用户路径。`electron-smoke.mjs` 是隔离组件/IPC/地图fixture测试，两者不得混称完整安装包验收。
