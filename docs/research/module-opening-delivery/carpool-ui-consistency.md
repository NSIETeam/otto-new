# 拼车界面一致性调整

2026-09-09，以电卡等园区服务表单为参照：标题加入图标和简短说明，移除大面积渐变宣传卡片，统一字段字号、输入框高度、圆角、留白和主发布按钮。拼车保留原模态弹窗交互，不新增不具备状态支持的最小化或最大化按钮。

修复地点搜索错位：原两列网格含有标签、输入框、按钮三个元素；first-of-type 还可能命中公共集合点区域。改为明确的搜索容器，标签保留屏幕阅读器关联，输入框和搜索按钮同排，定位和地图调整按钮独立排列。公共集合点使用独立样式。

只调整呈现和可选标题属性，未改搜索、定位、发布、匹配、草稿、权限及聊天业务。纯视觉变化未新增镜像实现的单元测试；保留原行为测试。

验证：
- 拼车、园区服务和通用弹窗共 87 项测试通过。
- renderer 类型检查、变更 TSX 文件零警告 lint、git diff --check 通过。
- renderer 编译通过；未打安装包。
- 当前 App 已加载新界面并打开拼车。自动读取实际布局：两组搜索框均与按钮同排，无重叠，输入框宽约 678px、按钮约 65px（当前窗口尺寸）。没有声称完成所有屏幕尺寸或人工视觉验收。

代码在 codex/blue-heron-7f3a9c 本地提交，不推送。
