# 测试地图默认配置

2026-09-09，按项目所有者明确要求，将其提供的高德 Web 服务测试 Key 作为代码默认值。Key 定义在 amapParkCarpoolProvider.ts，SQLite 与 PostgreSQL composition 统一读取。

- 环境变量 OTTO_AMAP_WEB_SERVICE_KEY 未设置：使用内置测试 Key。
- 环境变量非空：优先使用环境变量。
- 环境变量显式为空（包括空白字符）：关闭地图。
- 测试 Key 随代码发布，共享额度；这是本次用户明确选择。正式环境可以覆盖，无须修改调用逻辑。
- 既有服务器须更新后端代码才生效。仅分发客户端不会改变旧服务器配置，也不会自动提供企业服务器、测试账号或商业授权。

验证：

- 新增两项测试先红后绿，覆盖默认值、覆盖配置及显式关闭；原 Provider 11 项测试保留。
- 拼车目录：89 项通过，22 项因现有集成环境条件跳过，未修改跳过规则。
- 服务端 TypeScript 编译、变更文件 lint、doctor、code-map 和差异检查通过。
- 额外 enterprise/server.test.ts：99 通过、1 失败；常驻任务断言期望 6 项，实际包含拼车与跳蚤市场维护共 8 项。本次未修改该测试或任务注册实现，未将该检查记为通过。
- 不设置 Key 环境变量，直接使用编译后代码默认值：真实地点搜索返回 2 项；真实驾车路线返回距离 8256 米、预计 2049 秒（该次高德响应，非固定测试数据）。
- 本地 App 已保留原数据目录和服务器地址重启；IPC 确认 mapConfigured=true，真实搜索返回 2 项。
- 启动器避免对已有有效会话重复登录/重载；本轮重启后再次查询成功。此前出现的超时未再复现，未据此宣称已经完成长期稳定性测试。

本轮未打安装包，未验证远端部署，未推送 GitHub。日志保存在 /tmp/otto-amap-default-*.log；不在验证日志中打印 Key。
