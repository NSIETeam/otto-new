# 模块打开体验修复验收报告

日期：2026-09-09。代码基线：`0c075de0`，包含新版企业星链图。
工作目录：`/Users/yang/Desktop/otto-carpool-fixed-91bc4e`。
交付分支：`codex/blue-heron-7f3a9c`。按用户最终指令仅本地提交，不推送 GitHub。

## 结论

代码修复完成，最终两项独立代码复审未发现实质性阻断问题。桌面全量测试 219 个文件、1687 项测试通过；renderer/main/preload 类型检查及本次全部修改文件的零警告 lint 通过。

这不是生产上线或人工视觉验收结论。按用户要求未打包、未运行 App 真机人工视觉验收；未测真实网络下逐模块首屏毫秒数、所有屏幕尺寸或 Windows 实机。

## 逐项验收

| 工作 | 已交付行为 | 验证与限制 |
|---|---|---|
| 1. 窗口尺寸 | 园区紧凑表单620px、标准840px、复杂1080px上限；通用DialogFrame分档；Skill候选紧凑；最大化保持独立规则；视口约束和标题区域保留 | CSS与调用点审阅。视觉协调性和小屏截图由用户本地测试，不冒称实测 |
| 2. 七类表单首屏 | 装修、停车、网络与固话、会议室、电卡、报修、车辆访客立即渲染字段；身份验证控制提交；历史记录独立等待/失败重试；晚到默认值不覆盖已输入或主动清空字段 | 挂起历史请求时可输入、恢复默认值保护回归通过 |
| 3. 共享数据 | 当前登录工作区独立ModuleReadCache；园区、身份、工单、公告、资源等只读请求复用；并发合并、容量上限；写后失效；服务器/账号/企业key重建Provider，账号更新/会话失效清空缓存 | 并发、缓存失效、消费过期结果、跨实例隔离测试通过；未修改服务器授权 |
| 4. 重开复用 | 企业记忆、政策保留已读列表；市场复用查询与过滤快照；Skill保存匹配查询的结果及滚动；拼车打开采用稍后预热数据；图谱复用真实数据 | 重开时新请求挂起、缓存失败保留、切换账号不出现旧市场列表等通过 |
| 5. 输入与退出 | 最小化保留组件、输入和焦点；未提交表单需明确放弃；返回列表与关闭分别执行；问卷提交后解除未保存状态；拼车确认放弃后清空未发布草稿 | 输入→最小化→还原→确认返回、问卷提交→关闭、拼车放弃→重开回归通过 |
| 6. 状态表达 | 满意度读取中不再显示暂无问卷；身份和历史错误分开；Skill未知/失败不冒充空结果，并提供重试；已有列表后台更新不整屏清空 | 慢请求、失败及实际空数据路径测试/代码检查 |
| 7. 预加载 | 可用入口悬停/键盘聚焦后，仅预取公告、会议室、拼车、图谱、企业记忆等允许的只读查询；图谱画布保持代码拆分并可提前下载；现有能力读取结果进入共享缓存 | 不预执行发布、标记已读、模型任务、定位；预取许可与重复请求测试通过 |
| 8. 模块专属 | 会议室提交冲突立即失效并刷新资源；图谱保留新版探索、降级、公开资料和权限撤销逻辑；市场沿用既有持久草稿；Skill本机列表不等待远端投稿记录，投稿前仍确认已有状态；政策保护正在编辑的资料 | 原专项回归和新增时序测试通过；不把图谱已有demo模式当作真实数据验收 |
| 9. 其他入口兼容 | 招聘共享工作区、Agent激活、客户模块显式运行、管理模块和添加功能组保留原执行契约 | 桌面全量覆盖相关入口、会话、私聊、能力解析等；未修改core/server业务实现 |
| 10. 多窗口 | 激活层级保留；最小化和最大化分别处理；缩小视口时回到可见中心；窗口关闭归还焦点 | 状态及焦点测试通过；实际拖拽与视觉效果未人工验收 |
| 11. 权限与配置 | 保留开发账号、服务端门禁和原不可用原因；预取仅对available模块执行 | 能力解析专项与模块入口回归通过；未新增超级权限绕过或伪造依赖就绪 |
| 12. 验证与Git | 测试、类型、lint、代码映射、差异和独立审阅完成；本地分支提交 | 无打包、无GitHub推送 |

## 验证命令与结果

- `cd packages/desktop && ../../node_modules/.bin/vitest run`：219文件、1687测试通过（最终功能代码）；包含renderer/main/preload测试。
- `tsc -p packages/desktop/tsconfig.renderer.json --noEmit`：通过。
- `tsc -p packages/desktop/tsconfig.main.json --noEmit`：通过。
- `tsc -p packages/desktop/tsconfig.preload.json --noEmit`：通过。
- 本次所有变更TS/TSX文件 `eslint --max-warnings 0`：通过。
- `npm run doctor`：通过。此前源码体积包含被Git忽略的旧`tsconfig.tsbuildinfo`，已备份到`/tmp/otto-module-tsconfig-20260909.tsbuildinfo`后移出源码目录，未提高预算或修改检查规则。
- `npm run code-map:check`、`git diff HEAD --check`：通过。
- `npm run lint:ci`：仍有21个基线错误，全部位于本次未修改的星链图测试文件，详见下节。
- `npm run validate:boundaries`：仍有1个基线测试跨包引用违规，详见下节。

完整测试日志保留在本机`/tmp/otto-module-desktop-final.log`；提交内附精简验证记录。部分历史组件测试有React act时序提示，测试断言全部通过；未关闭测试、降低阈值或删除断言。

## 已有静态检查问题（未归为本次通过）

这些文件与`0c075de0`逐字一致，本次未修改：

- `packages/desktop/src/renderer/components/EnterprisePublicProfilePanel.test.tsx`
- `packages/desktop/src/renderer/components/EnterpriseStarMapView.test.tsx`
- `packages/desktop/src/renderer/starMap/EnterpriseList.test.tsx`
- `packages/server/src/enterprise/enterpriseStarMapRoutes.test.ts`

上述四个测试文件共21项lint错误，主要为unused React import、显式any和函数表达式风格。

`packages/desktop/src/renderer/policyIntelligencePresentation.test.ts`原有对服务端`policyDomain.js`的深路径引用触发包边界检查。其契约测试仍保留且通过，没有弱化边界规则或删除测试来制造全绿。

## 审阅中发现并修复

1. Skill新查询未完成时错存旧结果：增加结果有效状态，清除不匹配数据，新增离开重开测试。
2. 问卷提交成功仍标记dirty：增加显式保存回调，新增成功提交后关闭测试。
3. 工单过期读取可能覆盖写后状态：缓存有效性检查同时用于父级轮询和子表单消费端。
4. 拼车关闭状态挂载后错过预热：打开时读取最新缓存，仍保护正在编辑内容。
5. 身份尚未返回时保留表单输入能力，但申请提交、办理回复和验收动作均等待身份确认；新增挂起身份请求回归。
6. 企业记忆写后复用旧请求/已删条目恢复：成功写操作清除所有知识查询缓存，消费端验证有效性，新增删除后离线重开回归。

最后两项复审均未发现新的实质性阻断问题。测试能支持本次代码变更的回归结论，但不能证明所有真实部署、外部接口和设备场景都无问题。

## 分支与范围

最初按当时指令在新建本地main临时实施；用户改回原分支后，通过三方合并转移到`codex/blue-heron-7f3a9c`，保留刚提交的新版星链图，并重新验证。临时main上的本次改动已全部撤回，目录干净。未移动或覆盖原`otto-new`中的未提交团队代码，未合入远端internal的无关更新。
