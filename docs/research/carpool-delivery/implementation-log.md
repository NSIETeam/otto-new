# 拼车实施记录

## 范围与工作位置

基线提交 `93dcf8ea2dd0e9582589351a6e126ecd24b33b86`。用户授权独立分支 `codex/blue-heron-7f3a9c`，继续完成三个阶段后只做本地提交。原 `/Users/yang/Desktop/otto-new` 的 `internal` 分支及用户既有资料、native 产物不纳入本次提交。

开发目录 `/Users/yang/Desktop/otto-new-carpool` 出现另一跳蚤市场任务的共享入口改动。为避免覆盖协作工作，最终在 `/tmp/otto-carpool-validation-7f3a9c` 从基线建立独立验收副本，只带入拼车专属文件及共享文件中的拼车改动。验收和提交以该副本为准，另一任务文件保持原样。最终将验收副本移至 `/Users/yang/Desktop/otto-carpool-verified-7f3a9c` 并挂到用户的 `codex/blue-heron-7f3a9c` 分支；原混合目录保留在 `codex/preserved-mixed-8d61e2`，没有覆盖其文件。未改无关依赖、版本号、core 产品规则或企业内私聊隔离。

## 阶段一

修复长短路线中点整段计入导致的 100% 高估，改为有向局部区间并集，共同里程不超过短路线；修复北京时间日期不一致、跨午夜、无效日期、中文门牌泄露、下午时间和补充窗口解析、地点误选首项及取消/恢复草稿。

接通真实高德 Provider 的搜索、路线、逆地理编码、GPS 转换和服务端静态图，管理员公共集合点、地图选点、明确定位许可与手动降级。发布采用租约、幂等回执和版本校验，异步地图期间身份/园区/停止/删除变化不能复活旧意向。

完成分页召回/筛选/排序、新鲜度确认/暂停、新结果提示、共同出发时段及隐藏端点的路线对比。消息请求、接受后独立一对一 MLS 聊天、屏蔽/举报处理、通知/已读、自动维护、用户删除与运营指标均接入持久服务。

## 阶段二

结构化邀请明确方式、唯一司机、1/2/3 乘客容量；接受请求和创建两人组在事务中验证双方版本及单用户单组。拒绝、撤回、冷却、失效均有真实状态。保留组停止新匹配、退出并停止、取消分别处理。两人群使用独立会话，不复制原私聊；关闭多人开关仍可读已授权两人组历史。

## 阶段三

完成个人/组混排、每个外部候选独立计算司机重合/叫车公式及 Provider 绕行。组规划分批推进并送入有界结果排序，后段优质候选不被静默截断。入组与邀人入组、最后名额竞争、转交提议/接受、司机退出无人接任暂停、两人变一人关闭组均由服务端管理。

成员变化轮换代次和 MLS 密钥；真实 Native 测试验证新成员无旧历史、离开成员无新消息。增加本组路线入口、成员脱敏发送者标识、同组非直接请求成员屏蔽、单消息举报最小证据。HTTP 传输要求获批设备签名，scope/账号/命令/时效绑定，不能仅持会话对象 ID 访问。

## 自查修复与兼容

独立只读 reviewer 参与最终核对，已修复有依据的授权、UI 可达性、消息举报、旧邀请、组预览、新鲜度、统计口径及批处理问题。统计中历史迁移意向不计为当天发布，匹配比例只统计当天发布并匹配的账号日，避免超过 100%。结果卡增加已发送/收到待处理请求标识，避免诱导重复请求。

Native 包装探测从仅 ping 增为同时检查 park MLS，实际固定 toolchain 构建通过。现有企业私聊/联邦/附件/E2EE/MLS、模块工作区和 Inbox 做专项回归；公告/调查/工单/星链图完整产品手工激活没有逐一实测，报告中保留这一限制。

另补齐普通/自定义园区服务组的一次性拼车入口迁移：布局持久标记保留，能力暂不可用时延迟，已移到其他组的入口不搬回。移除旧的园区模块强制重排逻辑，以满足 PRD 保留其他模块顺序和分组；宏创新版本已迁移后主动移除的记录继续保留。真实解析回归先红后绿。

## 工程纪律与证据边界

多项业务缺陷先有失败回归再修复，失败日志保留于 evidence。后期部分展示字段、统计集成及共享入口归属整理未全部严格遵循先失败测试顺序；已补真实链路回归，但不能声称全过程 100% 测试先行。隔离验收副本重新执行针对性测试、类型检查、lint、构建和 Electron 流程。

测试使用虚构账号、显式地图 fixture 和临时数据库；生产代码使用真实 Provider，没有用 fixture 替代产品接口。真实高德服务、正式产品身份部署、Windows 和生产多节点未实测，详见最终自查。早期只完成基础修复的日志作为历史失败/阶段证据保留，不作为当前完整验收结论。

未推送 GitHub、未合并 internal、未部署、未联系真实用户发送测试请求。最终提交可用 `git log -1 codex/blue-heron-7f3a9c` 查询。
