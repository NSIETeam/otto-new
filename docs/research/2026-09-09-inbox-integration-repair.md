# 消息页接入修复

## 基准与范围

以真正默认分支 origin/internal（检查时 9d65ad94）核对原消息页。当前开发分支为 codex/blue-heron-7f3a9c。修复商品/拼车整块页面插入标题下方造成的挤占，不回退数据库，不修改服务器、主进程、IPC、消息协议、权限或加密实现。

## 完成

- 默认会话页恢复筛选 + 左侧会话目录 + 右侧详情。新增商品消息、同行消息分类，内容在独立滚动区域展示。
- 原会话列表、发送、历史、已读、联系人、跨企业核验、工单调用路径保留。
- 分类切换保留私聊和业务组件草稿；账号或组织切换清除业务分类状态。
- 原会话隐藏后不继续发起选中会话读取/已读处理，晚到结果不确认已读；返回后重新读取。
- 商品会话通过原 MarketContactCenter 内部线程入口打开，不再插入企业私聊正文和输入框之后，不再依赖对方出现在本企业成员目录。
- 同行使用 personal 模式，消息分类不展示园区统计和删除数据；拼车模块原有管理入口保留。
- 业务区域的标题、输入框、按钮、边框和留白使用现有主题变量。

## 验证

70 项相关测试通过：InboxPage 12，分类 3，MarketContactCenter 2，真实 SQLite + Native MLS 拼车流程 1，enterprise-mls-private-messages 6，enterprise-mls 32，enterprise-e2ee 14。分类新增测试先复现缺少隔离后再实施。私聊测试新增切换分类保留草稿及隐藏时晚到结果不回执已读的检查。

类型检查、修改文件 lint、renderer 构建、doctor、diff check、code-map check 通过。部分既有商品会话测试有 React act 提示，测试通过但未掩盖提示。

## 边界与未验证

- 分类目前分别展示消息，未将商品/同行条目混入原会话总数；原“全部/未读/已读”仅作用于会话分类。没有声称完成全类型统一未读目录。
- origin/internal 分叉后新增的政策消息及其后端依赖尚未同步，本次未摘取不完整政策组件。当前分支不能宣称完全等同最新 internal；该差异不是此次修复引入。
- 未做 App 人工视觉验收、实际跨部署网络双人验收、打包或部署。没有重启用户的运行实例。
- 未纳入其他人在企业星链图上的未提交修改。
