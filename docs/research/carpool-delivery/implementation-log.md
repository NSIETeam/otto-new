# 拼车修复实施日志（第 2 轮最终代码交付）

**已确认代码缺陷已修复并通过相关自动化验证；按用户最新要求不再继续真机验收，仅本地提交。** [最终自查](final-self-review.md) 是本轮结论；[逐项矩阵](acceptance-matrix.md) 保留未验证项，不以“有实现”冒充完整产品验收。

用户最后明确收窄交付范围：**不需要真机验收，完成代码修改并本地提交**。因此真机、真实地图、Windows、正式部署与运营签认不再作为本轮代码交付的阻塞项；未执行项目仍如实保留，不标成通过。

本轮开始 HEAD 为 `cecf75a1`，分支 `codex/blue-heron-7f3a9c`，开始时本工作目录干净。随后其他任务在同目录产生跳蚤市场等修改；本轮只提交拼车文件及共享文件中的拼车 hunk，不包含其他任务新增 hunk。测试运行于此共享工作目录，不声称是所有未提交工作都已验收。

开始时间、Node v24.4.0、npm 11.5.2、两个目录状态见 [原始基线](evidence/repair-pass-2-baseline.json)。原目录是不同版本/混合未提交工作，不覆盖其产品代码。旧五份报告分别保存 branch/original 快照于 `history/2026-09-08-before-repair-pass-2/`。

| Task | 实际完成与界限 |
|---|---|
| 0 | 全文读取需求、提示词、规则和历史；重建基线。原三路由已被既有提交修复；doctor 分目录记录 |
| 1 | 28 路由断言＋真实 HTTP；未知内部异常红测2项后修复 |
| 2 | 真实 Native 路径红测、root/workspace 各6绿；支持Windows文件名，未声称Windows实跑 |
| 3 | 缺省关闭、显式快照、园区试点、暂停发现/历史管理；双数据库红绿回归 |
| 4 | 最终服务端137通过，无跳过；worker长租约、失租取消、重复通知/指标补修 |
| 5 | 桌面47、原回归185、联邦9、隔离Electron通过；正常App标准启动/登录通过；三人完整旅程不在用户最后要求内 |
| 6 | Provider错误11项；公共点 live 脚本准备，缺key实际返回阻塞；按最新要求停止真机验收 |
| 7 | types/build/lint/code-map通过；Rust23真实测试；已知政策模块边界失败及原目录体积超限保留 |
| 8 | 五份报告重写、367+17原文编号核对、非列表51操作步骤另外追踪；未把相关测试批量标验收通过 |
| 9 | 独立review与9问见最终自查；只提交本任务hunk，不push |

每个中途失败日志均保留：Native相对cwd、配置测试漏注入、桌面测试helper编译方式、HTTP异常、worker取消传播、延迟租约及地图响应。最终服务端第一次141项中2失败系新增基线service漏显式测试配置；只补注入，原race断言保留。第二次标准拼车命令137通过，差额4项是前次额外执行SharedCache；它们另在9项基础设施回归中通过。

本轮未改原企业私聊核心业务、未合并市场任务hunk。最新本地提交可用 `git log -1 --oneline` 核对；提交范围与留下的其他改动见交付时 Git 记录。本地提交不包含生产操作授权。

## 本地开发入口后续修复

用户实际打开App后发现：缺地图配置导致已授权拼车入口被隐藏，且之前测试园区身份不匹配宏创模板。已修正入口判定：后端明确启用且园区授权有效时允许打开面板，地图缺失只限制发布；非试点仍隐藏。新增红测后26项相关回归通过，desktop构建和定向lint通过，见 evidence/local-dev-admin-entry-red.log、local-dev-admin-entry-green.log、local-dev-admin-build.log。

重新创建独立本地开发企业/园区管理员、全部8项功能授权，并配置匹配模板的本地园区；平台管理令牌与测试账号仅保存在仓库外专用文件，不提交凭据。实际App已登录并成功添加园区功能组、打开拼车助手；不把外部地图缺配置当权限问题，也没有取消生产鉴权。
